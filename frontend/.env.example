# API Configuration
VITE_API_BASE_URL=http://localhost:8080/api/v1

# Stripe Configuration (test mode, publishable key is safe to expose)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Payment Mode: 'mock' or 'stripe'
VITE_PAYMENT_MODE=mock
