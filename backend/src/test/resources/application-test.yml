spring:
  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

  ai:
    anthropic:
      enabled: false
    openai:
      enabled: false
    ollama:
      enabled: false
    vectorstore:
      pgvector:
        enabled: false
        initialize-schema: false

logging:
  level:
    com.mockhub: DEBUG
    org.testcontainers: INFO
